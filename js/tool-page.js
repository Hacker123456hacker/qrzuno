document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('[data-qr-form]');const out=document.querySelector('#qr-output');if(!form||!out)return;form.addEventListener('submit',e=>{e.preventDefault();const value=new FormData(form);const text=[...value.values()].filter(Boolean).join(' | ');out.textContent=text||'Enter information to generate your QR code.';});});